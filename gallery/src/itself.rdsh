dig drawing plant new Drawing(810, 240)
dig text plant system.readFile(PATH)
drawing.string(text, 0, 19, 10, t(x, y) {
    h new Drawing.RGB(math.floor(x / 4),
        127 - math.floor(x / 8), y)
})
harvest drawing